import React, { useState } from "react";

const PostTestView = (props) => {
  const [state, setstate] = useState(initialState);

  return <div></div>;
};

PostTestView.propTypes = {};

export default PostTestView;
